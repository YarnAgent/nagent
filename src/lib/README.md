---
purpose: Cross-cutting utilities used by multiple feature modules.
---

# lib/

Small, well-scoped helpers that aren't tied to any single feature/domain.

- `canonical.ts` — deterministic JSON for signing (used by invite + gossip).
