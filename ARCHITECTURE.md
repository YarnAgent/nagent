# Architecture Map

_Auto-generated by `scripts/build-arch-map.sh` (runs in `.githooks/pre-commit`). To update an entry, edit the `purpose:` frontmatter in that directory's `README.md` and commit._

| Path | Purpose |
|------|---------|
| `.githooks/` | Tracked git hooks (enable via `git config core.hooksPath .githooks`). |
| `docs/` | Project documentation — PRD, system design, ADRs, architecture map sources. |
| `docs/architecture/` | Architecture overview and decision records (ADRs). |
| `docs/architecture/adr/` | Architecture Decision Records — immutable log of significant choices. |
| `scripts/` | Project automation scripts (build, codegen, hooks helpers). |
| `src/` | Source code, organized by feature/domain (not by file type). |
| `src/bus/` | Inter-session bus — length-prefixed JSON frames over Unix socket, verbs, pattern matcher. |
| `src/cli/` | `nagent` CLI surface (commander) — entry point, picker, all v0.1 verbs, deferred-verb stubs. |
| `src/daemon/` | `nagentd` long-running process — owns the bus socket, subscriber registry, and session catalog. |
| `src/invite/` | Invite token codec — Iroh-/Tailscale-shaped tokens with a one-time SSH key for the join handshake. |
| `src/platform/` | Platform abstraction — paths, launchd/systemd unit writers, perm enforcement. |
| `src/project/` | Project concept — net-wide grouping; `.nagent` marker reader/writer; cwd-walker. |
| `src/session/` | Session management — thin wrapper over `tmux -L nagent` (new/attach/list/close). |
| `src/ssh/` | SSH wiring — authorized_keys fenced editor, ssh_config writer, identity helpers, reachable-address detection. |
| `src/store/` | Flat-JSON disk store — atomic rename writes, permission enforcement. |
| `src/types/` | Shared TypeScript types — identities, nets, projects, sessions, bus frames. |
| `tests/` | Test suites — unit, integration, and end-to-end. Mirrors `src/` layout. |
| `tests/integration/` | _(no `purpose:` set in `tests/integration/README.md`)_ |
| `tests/unit/` | _(no `purpose:` set in `tests/unit/README.md`)_ |
