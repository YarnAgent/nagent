# Architecture Map

_Auto-generated by `scripts/build-arch-map.sh` (runs in `.githooks/pre-commit`). To update an entry, edit the `purpose:` frontmatter in that directory's `README.md` and commit._

| Path | Purpose |
|------|---------|
| `.claude/` | _(no `purpose:` set in `.claude/README.md`)_ |
| `.claude/agents/` | _(no `purpose:` set in `.claude/agents/README.md`)_ |
| `.githooks/` | Tracked git hooks (enable via `git config core.hooksPath .githooks`). |
| `dist/` | _(no `purpose:` set in `dist/README.md`)_ |
| `dist/bus/` | _(no `purpose:` set in `dist/bus/README.md`)_ |
| `dist/cli/` | _(no `purpose:` set in `dist/cli/README.md`)_ |
| `dist/daemon/` | _(no `purpose:` set in `dist/daemon/README.md`)_ |
| `dist/gossip/` | _(no `purpose:` set in `dist/gossip/README.md`)_ |
| `dist/invite/` | _(no `purpose:` set in `dist/invite/README.md`)_ |
| `dist/lib/` | _(no `purpose:` set in `dist/lib/README.md`)_ |
| `dist/platform/` | _(no `purpose:` set in `dist/platform/README.md`)_ |
| `dist/project/` | _(no `purpose:` set in `dist/project/README.md`)_ |
| `dist/session/` | _(no `purpose:` set in `dist/session/README.md`)_ |
| `dist/ssh/` | _(no `purpose:` set in `dist/ssh/README.md`)_ |
| `dist/store/` | _(no `purpose:` set in `dist/store/README.md`)_ |
| `dist/types/` | _(no `purpose:` set in `dist/types/README.md`)_ |
| `dist/web/` | _(no `purpose:` set in `dist/web/README.md`)_ |
| `dist/web/static/` | _(no `purpose:` set in `dist/web/static/README.md`)_ |
| `dist/web/static/vendor/` | _(no `purpose:` set in `dist/web/static/vendor/README.md`)_ |
| `docs/` | Project documentation — PRD, system design, ADRs, architecture map sources. |
| `docs/architecture/` | Architecture overview and decision records (ADRs). |
| `docs/architecture/adr/` | Architecture Decision Records — immutable log of significant choices. |
| `scripts/` | Project automation scripts (build, codegen, hooks helpers). |
| `src/` | Source code, organized by feature/domain (not by file type). |
| `src/bus/` | Inter-session bus — length-prefixed JSON frames over Unix socket, verbs, pattern matcher. |
| `src/cli/` | `nagent` CLI surface (commander) — entry point, picker, all v0.1 verbs, deferred-verb stubs. |
| `src/daemon/` | `nagentd` long-running process — owns the bus socket, subscriber registry, and session catalog. |
| `src/gossip/` | Mesh-trust extension — signed gossip-add-peer protocol that distributes new peer pubkeys across the net so any-to-any direct SSH attach works without re-inviting (v0.3+). |
| `src/invite/` | Invite token codec — Iroh-/Tailscale-shaped tokens with a one-time SSH key for the join handshake. |
| `src/lib/` | Cross-cutting utilities used by multiple feature modules. |
| `src/platform/` | Platform abstraction — paths, launchd/systemd unit writers, perm enforcement. |
| `src/project/` | Project concept — net-wide grouping; `.nagent` marker reader/writer; cwd-walker. |
| `src/relay/` | nagent-relay v0.5 — TLS-fronted dumb-pipe TCP rendezvous; clients register over a long-lived TLS conn and ask the relay to open opaque byte-streams between two registered peers. SSH stays end-to-end encrypted across the relay. |
| `src/session/` | Session management — thin wrapper over `tmux -L nagent` (new/attach/list/close). |
| `src/ssh/` | SSH wiring — authorized_keys fenced editor, ssh_config writer, identity helpers, reachable-address detection. |
| `src/store/` | Flat-JSON disk store — atomic rename writes, permission enforcement. |
| `src/types/` | Shared TypeScript types — identities, nets, projects, sessions, bus frames. |
| `src/web/` | nagent-web hub — HTTPS server that gives browsers access to tmux sessions across the mesh by SSH-tunnelling to a per-session ttyd on the owning peer (v0.4+). |
| `src/web/static/` | _(no `purpose:` set in `src/web/static/README.md`)_ |
| `src/web/static/vendor/` | _(no `purpose:` set in `src/web/static/vendor/README.md`)_ |
| `tests/` | Test suites — unit, integration, and end-to-end. Mirrors `src/` layout. |
| `tests/integration/` | _(no `purpose:` set in `tests/integration/README.md`)_ |
| `tests/unit/` | _(no `purpose:` set in `tests/unit/README.md`)_ |
